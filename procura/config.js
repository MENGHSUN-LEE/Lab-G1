// config.js
const config = {
  db: {
    host: "assignment-mysql", 
    user: "root",
    password: "417", 
    database: "assignment_db", 
    connectTimeout: 60000 
  },
};

module.exports = config;